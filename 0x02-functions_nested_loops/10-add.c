#include "main.h"
/**
 * add - adds to integers and returns result
 *
 * @a: input as integer
 * @b: input as integer
 * Return: result of a + b
 */
int add(int a, int b)
{
int sum;
sum = a + b;
return (sum);
}
